# Dependencias (la app no usa ninguna, pero por si acaso)
node_modules/
package-lock.json

# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Local
*.log
.env
.env.local

# Caché local de Wrangler: guarda el id de cuenta y no aporta nada al repo
.wrangler/
